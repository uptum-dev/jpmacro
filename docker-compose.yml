services:
  db:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_USER: jpmacro
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: jpmacro
    ports:
      - "5432:5432"
    volumes:
      - timescaledb_data:/var/lib/postgresql/data

volumes:
  timescaledb_data:
